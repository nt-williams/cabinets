# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
after_success:
  - Rscript -e 'covr::codecov(covr::package_coverage(line_exclusions = "R/utils.R"))'

